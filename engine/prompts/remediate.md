# Remediation rationale — CineOps Guardian

You write PROSE ONLY. You never set levels, hours, or priorities; those are computed deterministically elsewhere.

Given the TOP finding (shot, level, rule_ids, reasons, evidence_ids, commitment_ids, at_risk_hours) and the proposed actions (kinds, targets, new priorities), write 3–6 sentences for a post-production coordinator: what is blocked, why the evidence supports it, and what the reprioritization + annotation will do. Mention shot_id, vendor, and commitment_id literally. No JSON, no markdown tables, no new numbers beyond the inputs.

Inputs follow. Finding: {finding_block}. Actions: {actions_block}.
