# CineOps Guardian — env template (NFR-06: names + EMPTY values only; copy to .env and fill; never commit secrets).
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=
GOOGLE_GENAI_USE_VERTEXAI=
GEMINI_MODEL=
GEMINI_TEMPERATURE=
BQ_DATASET=
GRAFANA_STACK_URL=
GRAFANA_MCP_URL=
GRAFANA_SERVICE_ACCOUNT_TOKEN=
GRAFANA_TRANSPORT=
# Optional: explicit path to the grafana/mcp-grafana binary for
# GRAFANA_TRANSPORT=stdio. Leave empty to resolve it on PATH.
MCP_GRAFANA_BIN=
COST_BUDGET_USD=
RES_FORCED_DEGRADED=
API_BASE=
